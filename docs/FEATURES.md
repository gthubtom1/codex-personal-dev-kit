# Feature Map

This is the current behavior of Codex Personal Dev Kit. It is an index, not a development log.

| ID | User capability | Entry points / connected path | Expected result | Verification | Criticality | Status |
| --- | --- | --- | --- | --- | --- | --- |
| DK-001 | Start software work with one plain-language request | Global AGENTS route -> `codex-development-assistant` -> requirement expansion -> automatic Git readiness -> persistent Goal for explicit multi-step completion requests | Codex expands the need and continues to a usable, verified, locally recoverable result with only material decisions exposed; the user need not name Skills, Goal, or Git. | suite:all-tests; tests/test_install_scripts.py; tests/test_feature_guard.py | critical | active |
| DK-002 | Create and open independent long-term projects | Configurable mother folder or existing-project onboarding -> deterministic installed-runtime/source bootstrap resolution -> path-templated AGENTS/config/docs -> docs/INDEX navigation map -> generated/path/content secret preflight -> local Git baseline -> open project folder | Each project is an independent Codex working folder and Git repository with a first local recovery point; onboarding uses an exact local bootstrap script without guessing or searching, current project facts have a short documentation entry point, existing source is preserved, custom workspace roots resolve correctly, lifecycle Hooks are not installed, known secret files stay ignored, and suspicious credential content stops the baseline. | suite:all-tests; tests/test_workspace_scripts.py; tests/test_install_scripts.py | critical | active |
| DK-003 | Change one feature without silently losing another | Aggregated FEATURES IDs -> current change contract -> explicit guard-managed staging -> executed verification/content fingerprint -> checkpoint | Protected records, undeclared scope/deletions, mixed user changes, fabricated evidence, raw commits, post-verification changes, and verified-but-unsaved completion are detected by the explicit guard workflow. | suite:all-tests; tests/test_feature_guard.py | critical | active |
| DK-004 | Recover from mistakes without learning Git first | Safe development Skill -> current branch -> exact-tree checkpoints -> clean-worktree reversible rollback -> explicit guard scripts | Every verified slice gets a local recovery point without version branches; “return to the previous version” creates a new recovery commit while destructive/remote actions remain blocked. | suite:all-tests; tests/test_feature_guard.py; tests/test_pre_tool_guard.py | critical | active |
| DK-005 | Continue across shorter Codex tasks without giant documents | startup/resume/compact/clear recovery packet -> docs/INDEX -> AGENTS/PROJECT/FEATURES/STATUS -> linked feature/architecture/ADR docs -> Git/tests -> continuity Skill -> navigation audit | Recovery preserves a bounded `Next Action`, follows only task-relevant documentation links, detects broken or orphaned durable docs, verified-but-uncommitted work remains protected, and any oversized text document under `docs/` is reported without relying on its filename. | suite:all-tests; tests/test_feature_guard.py; tests/test_audit_project.py | standard | active |
| DK-006 | Use native subagents without creating task or merge chaos | Orchestration Skill -> current-task native `spawn_agent` -> task receipt confirmation -> one writer per checkout -> Worktree only for real isolation | Bounded native subagents can assist when useful; the Dev Kit never configures or overrides their model, reasoning, concurrency, enablement, or interruption settings, failed starts are reported, unreadable task payloads are detected, and no Hook, replacement protocol, custom Agent, or visible task substitutes for a subagent. | suite:all-tests; tests/test_orchestration_routing.py | standard | active |
| DK-007 | Validate and install the assistant predictably | Fixed clean local checkout -> managed-marker preflight -> standalone prepare/verify -> metadata-driven update -> source/version/integrity diagnostics -> documentation index template; optional legacy cleanup only when explicitly requested | Standalone mode preserves unrelated global AGENTS and user Hook content, never changes `config.toml` or native subagent settings, rejects damaged markers and unsupported source layouts, detects old Plugin/custom-agent state without installing replacements, remembers custom workspace roots, verifies installed managed-file integrity, removes only unchanged obsolete Dev Kit files, and leaves native Codex capabilities untouched. | suite:all-tests; tests/test_install_scripts.py; tests/test_workspace_scripts.py; tests/test_audit_project.py | standard | active |
| DK-008 | Find project memory without reading every document | Project `docs/INDEX.md` -> STATUS/FEATURES -> linked feature, architecture, runbook, and ADR documents -> Markdown navigation audit | Durable project facts have a short entry point, stable relative links, bounded navigation depth, no broken local targets or anchors, and no unreferenced durable documents hidden from a fresh Codex task. | suite:all-tests; tests/test_audit_project.py; tests/test_workspace_scripts.py | standard | active |
| DK-009 | Resolve installed Skills and runtime scripts consistently across Codex tasks | Current task Skill metadata -> exact Skill locator -> exact standalone runtime path -> pinned source metadata -> mother-folder source fallback -> explicit discovery-layer diagnostic | Codex does not invent a `.system` prefix, treat a Skill folder as the runtime root, recursively search the machine, or download a replacement; it distinguishes path construction errors from missing installation and keeps the same nine Skills and onboarding script usable after a new task or standalone update. | suite:all-tests; tests/test_install_scripts.py; tests/test_orchestration_routing.py; tests/test_research_and_integration.py | standard | active |
| DK-010 | Research trustworthy existing solutions before building | Plain-language request/current-risk trigger -> `research-and-reuse` -> current-project constraints -> official/Web/GitHub evidence -> license/security/compatibility/exit evaluation -> safe-development handoff | Codex proactively researches when outside evidence is materially useful, prefers authoritative sources, treats external content as untrusted, does not leak private project data, distinguishes learning/dependency/source adaptation/prototype/self-build, requires approval before download/install/copy/private access, and stores only adopted long-term facts. | suite:all-tests; tests/test_research_and_integration.py | standard | active |
| DK-011 | Integrate capabilities from explicitly selected projects safely | Current target project -> `integrate-codex-projects` -> explicit read-only source paths -> compatibility matrix -> smallest capability slice -> feature protection/tests/checkpoint | Codex never scans unnamed sibling projects, keeps the open project as the only default write target, does not merge folders or Git histories by default, evaluates license/data/dependency conflicts, integrates through stable interfaces in verified slices, and keeps temporary integration planning bounded. | suite:all-tests; tests/test_research_and_integration.py | standard | active |
| DK-012 | Identify and restore formal versions without knowing Git or remembering version numbers | Verified milestone -> concise `docs/VERSIONS.md` capability row -> final checkpoint -> immutable local semantic tag -> capability-based lookup -> guarded named-version restoration | Ordinary checkpoints stay automatic and unlabeled; accepted milestones receive local `vX.Y.Z` labels without version branches or remote publication. Codex can match a user's feature description to candidate versions, list differences, and restore a confirmed tag with a new checkpoint while preserving newer history, tags, and the complete version index. | suite:all-tests; tests/test_feature_guard.py; tests/test_audit_project.py; tests/test_workspace_scripts.py; tests/test_pre_tool_guard.py | critical | active |

Keep this file concise. Add a feature only when it is a stable user capability with a repeatable verification path.
