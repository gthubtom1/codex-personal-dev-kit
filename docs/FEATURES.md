# Feature Map

This is the current behavior of Codex Personal Dev Kit. It is an index, not a development log.

| ID | User capability | Entry points / connected path | Expected result | Verification | Criticality | Status |
| --- | --- | --- | --- | --- | --- | --- |
| DK-001 | Start software work with one plain-language request | Short global AGENTS -> canonical detailed mother-folder AGENTS -> `codex-development-assistant` -> requirement expansion -> automatic Git readiness -> persistent Goal for explicit multi-step completion requests | Codex expands the need and continues to a usable, verified, locally recoverable result with only material decisions exposed; the user need not name Skills, Goal, or Git. Restoring the fixed GitHub version rebuilds the same complete beginner-facing contract on another computer. | suite:all-tests; tests/test_install_scripts.py; tests/test_feature_guard.py | critical | active |
| DK-002 | Create and open independent long-term projects | Fixed standalone checkout -> blank/existing configurable mother folder -> linked global/mother AGENTS bootstrap -> deterministic installed-runtime/source project bootstrap resolution -> path-templated project AGENTS/config/docs -> docs/INDEX navigation map -> generated/path/content secret preflight -> local Git baseline -> open project folder | A blank computer gets the mother-folder skeleton and detailed rules without manual setup, while existing workspace rules remain intact. Each project is then an independent Codex working folder and Git repository with a first local recovery point; onboarding uses an exact local bootstrap script without guessing or searching, current project facts have a short documentation entry point, existing source is preserved, custom workspace roots resolve correctly, lifecycle Hooks are not installed, known secret files stay ignored, and suspicious credential content stops the baseline. | suite:all-tests; tests/test_workspace_scripts.py; tests/test_install_scripts.py | critical | active |
| DK-003 | Change one feature without silently losing another | Aggregated FEATURES IDs -> current change contract -> explicit guard-managed staging with credential/large-file/lockfile checks -> executed feature-bound verification/content fingerprint -> checkpoint | Protected records, undeclared scope/deletions, mixed user changes, fabricated evidence, unbound verification runs, raw commits, post-verification changes, and verified-but-unsaved completion are detected by the explicit guard workflow. Staging refuses likely credentials outright and warns about oversized binaries and manifest changes whose existing lockfile is left behind; PowerShell/batch/shell scripts count as source behavior. | suite:all-tests; tests/test_feature_guard.py | critical | active |
| DK-004 | Recover from mistakes without learning Git first | Safe development Skill -> current branch -> exact-tree checkpoints -> clean-worktree reversible rollback -> explicit guard scripts | Every verified slice gets a local recovery point without version branches; “return to the previous version” creates a new recovery commit while destructive/remote actions remain blocked. | suite:all-tests; tests/test_feature_guard.py; tests/test_pre_tool_guard.py | critical | active |
| DK-005 | Continue across shorter Codex tasks without giant documents | startup/resume/compact/clear recovery packet -> docs/INDEX -> AGENTS/PROJECT/FEATURES/STATUS -> linked feature/architecture/ADR docs -> Git/tests -> continuity Skill -> navigation audit | Recovery preserves a bounded `Next Action`, follows only task-relevant documentation links, detects broken or orphaned durable docs, verified-but-uncommitted work remains protected, and any oversized text document under `docs/` is reported without relying on its filename. | suite:all-tests; tests/test_feature_guard.py; tests/test_audit_project.py | standard | active |
| DK-006 | Use native subagents without creating task or merge chaos | Orchestration Skill -> current-task native `spawn_agent` -> task receipt confirmation -> one writer per checkout -> Worktree only for real isolation | Bounded native subagents can assist when useful; the Dev Kit never configures or overrides their model, reasoning, concurrency, enablement, or interruption settings, failed starts are reported, unreadable task payloads are detected, and no Hook, replacement protocol, custom Agent, or visible task substitutes for a subagent. | suite:all-tests; tests/test_orchestration_routing.py | standard | active |
| DK-007 | Validate, distribute, and install the assistant predictably | Fixed clean Git checkout -> MIT license and third-party notices -> source/template/managed-marker preflight -> canonical complete Chinese workspace template -> blank-workspace preview/apply -> standalone prepare/verify -> metadata-driven update -> source/version/integrity and workspace-drift diagnostics -> two AGENTS templates, Skills, runtime, project templates, restore guide and tests; optional legacy cleanup only when explicitly requested | A fixed public GitHub version can rebuild the short global AGENTS, the complete detailed mother-folder AGENTS, workspace skeleton, nine Skills, central runtime and project templates on a blank Windows computer without pre-created files. The rendered detailed rules exactly match the canonical template for that computer. Preview writes nothing; apply preserves an existing customized mother-folder AGENTS instead of overwriting it and reports drift without printing its contents; repeated installation is idempotent. Standalone mode preserves unrelated global AGENTS and user Hook content, never changes global `config.toml` or native subagent settings, rejects damaged markers and unsupported source layouts, detects old Plugin/custom-agent state without installing replacements, remembers custom workspace roots, verifies installed managed-file integrity, removes only unchanged obsolete Dev Kit files, and leaves native Codex capabilities untouched. | suite:all-tests; tests/test_install_scripts.py; tests/test_workspace_scripts.py; tests/test_audit_project.py | standard | active |
| DK-008 | Find project memory without reading every document | Project `docs/INDEX.md` -> STATUS/FEATURES -> linked feature, architecture, runbook, and ADR documents -> Markdown navigation audit | Durable project facts have a short entry point, stable relative links, bounded navigation depth, no broken local targets or anchors, and no unreferenced durable documents hidden from a fresh Codex task. | suite:all-tests; tests/test_audit_project.py; tests/test_workspace_scripts.py | standard | active |
| DK-009 | Resolve installed Skills, runtime scripts, and detailed workspace rules consistently across users and Codex tasks | Current task Skill metadata -> installer-resolved Codex Home -> exact Skill locator -> exact standalone runtime path -> pinned source metadata -> canonical workspace-template rendering -> generated mother-folder source fallback -> explicit discovery-layer diagnostic | The installer substitutes the new computer's actual Codex Home, workspace name, workspace root and fixed local Skill source instead of retaining the original Windows username or requiring a hand-written link, and proves the live detailed rules were rendered from the fixed canonical template. Codex does not invent a `.system` prefix, treat a Skill folder as the runtime root, recursively search the machine, or download a replacement; it distinguishes path construction errors from missing installation and keeps the same nine Skills, onboarding script and complete mother-folder contract usable after a new task or standalone update. | suite:all-tests; tests/test_install_scripts.py; tests/test_orchestration_routing.py; tests/test_research_and_integration.py | standard | active |
| DK-010 | Research trustworthy existing solutions before building | New-capability default / plain-language request / current-risk trigger -> `research-and-reuse` -> current-project constraints -> official/Web/GitHub evidence -> license/security/compatibility/exit evaluation -> safe-development handoff | Codex researches by default before implementing any non-trivial new capability, integration, or unfamiliar error (learning approaches instead of wholesale merging), prefers authoritative sources, treats external content as untrusted, does not leak private project data, distinguishes learning/dependency/source adaptation/prototype/self-build, requires approval before download/install/copy/private access, and stores only adopted long-term facts. | suite:all-tests; tests/test_research_and_integration.py | standard | active |
| DK-011 | Integrate capabilities from explicitly selected projects safely | Current target project -> `integrate-codex-projects` -> explicit read-only source paths -> compatibility matrix -> smallest capability slice -> feature protection/tests/checkpoint | Codex never scans unnamed sibling projects, keeps the open project as the only default write target, does not merge folders or Git histories by default, evaluates license/data/dependency conflicts, integrates through stable interfaces in verified slices, and keeps temporary integration planning bounded. | suite:all-tests; tests/test_research_and_integration.py | standard | active |
| DK-012 | Identify and restore formal versions without knowing Git or remembering version numbers | Verified milestone -> 21-dimension release review in `docs/RELEASE-REVIEW.md` -> concise `docs/VERSIONS.md` capability row -> synchronized `package.json`/`VERSION` field -> final checkpoint -> immutable local semantic tag -> capability-based lookup -> guarded named-version restoration | Ordinary checkpoints stay automatic and unlabeled; accepted milestones receive local `vX.Y.Z` labels only when existing version fields match and a completed 21-dimension release review records that exact version in the tagged checkpoint (or in the current checkpoint, only when the backfilled historical tree carries no review of its own; a conflicting review inside the target always refuses the tag), with honest per-dimension statuses instead of fabricated evidence, without version branches or remote publication. Codex can match a user's feature description to candidate versions, safely inspect existing tags, list differences, and restore a confirmed tag with a new checkpoint while preserving newer history, tags, and the complete version index; raw tag mutation remains blocked. | suite:all-tests; tests/test_feature_guard.py; tests/test_audit_project.py; tests/test_workspace_scripts.py; tests/test_pre_tool_guard.py | critical | active |
| DK-013 | Back up accepted versions remotely without learning Git | Plain-language authorization -> exact existing remote URL/current branch/formal tags -> clean verified checkpoint -> guarded dry-run atomic push -> remote ref verification | Codex performs the exact authorized branch/tag backup itself and reports the result. Raw or force push, remote-ref deletion/movement, unlisted refs, Release creation and deployment remain blocked; conflicting remote tags are never overwritten. | suite:all-tests; tests/test_feature_guard.py; tests/test_pre_tool_guard.py | critical | active |
| DK-014 | Synchronize and integrate simple Git histories without handling Git commands | Explicit remote authorization -> guarded exact fetch/current-branch fast-forward; or exact local source branch -> guarded linear fast-forward | Remote or local history advances only when one side is a direct ancestor of the other and every accepted target is a Dev Kit checkpoint. Local-ahead state is preserved; divergent histories are never auto-merged or rebased and are routed to a separate conflict-resolution task. | suite:all-tests; tests/test_feature_guard.py; tests/test_pre_tool_guard.py | critical | active |
| DK-015 | Correct mistaken staging without losing edited files | Open current-change contract -> exact guard-staged paths -> guarded unstage -> verification invalidation | Codex can remove only its own explicitly staged files from the Git index while preserving working content. Unknown/user paths are refused and stale verification evidence is cleared. | suite:all-tests; tests/test_feature_guard.py; tests/test_pre_tool_guard.py | standard | active |
| DK-016 | Clean up stale Worktrees without deleting unique work | Exact registered path -> clean/ignored/untracked inspection -> integrated-commit proof -> non-force removal -> registry verification | Codex removes only a non-current, unlocked Worktree with no modified, untracked, ignored, or unique committed content. The branch remains; unsafe Worktrees are preserved automatically. | suite:all-tests; tests/test_feature_guard.py; tests/test_pre_tool_guard.py | critical | active |
| DK-017 | Install an approved Windows development tool without running commands manually | Exact winget package ID/version/scope authorization -> official-source show -> guarded install -> installed-version verification | Codex performs an idempotent first installation itself. Raw managers, wildcard/unpinned packages, implicit upgrades/downgrades, source substitution and unverified completion remain blocked. | suite:all-tests; tests/test_global_tool_guard.py; tests/test_install_scripts.py; tests/test_pre_tool_guard.py | critical | active |
| DK-018 | Ask for the exact next step instead of re-reading everything | Any task moment -> read-only `next_step.py` -> Git/docs/contract/version state -> ordered NOW/NEXT commands | The bundled script reports the current guarded stage and prints the exact next commands (onboarding, contract start, stage, verify, complete, checkpoint, pending formal versions and their 21-dimension release review) without modifying the project. Unregistered user changes are flagged for ownership confirmation before any contract advice, so prose workflows cannot be silently reordered or skipped. | suite:all-tests; tests/test_next_step.py | standard | active |

Keep this file concise. Add a feature only when it is a stable user capability with a repeatable verification path.
